# Develop: evidence

Read `workflows/shared/evidence.md`.

Capture accomplishments as evidence records. Check for duplicates based on context, action, and result. Separate confirmed public/shareable facts from confidential context.

Use:

```bash
python -m pco evidence add --input <json-file>
```

