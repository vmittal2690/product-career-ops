# Search: compare

Query current Active opportunities:

```bash
python -m pco opportunity list --status Active --json
```

Compare mandate, trajectory, evidence match, company/product quality, practical constraints, and uncertainty. Do not rank solely by headline score. Identify what would change the ranking.

